import Foundation

enum Constants {
    enum API {
        static let normalEndpoint = "recipes.json"
        static let malformedEndpoint = "recipes-malformed.json"
        static let emptyEndpoint = "recipes-empty.json"
    }
}

